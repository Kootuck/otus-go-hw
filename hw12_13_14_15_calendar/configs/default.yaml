logger:
  level: "error" # error | warn | info | debug
storage:
  method: "in-memory" # in-memory | sql
  pg:
    dbUser: "otus_hw_user"
    dbPassword: "password"
    dbName: "otus_hw_db"
    dbHost: "localhost"
    dbPort: "5432"
    execDefaultTimeoutSeconds: 5
http:
  host: "localhost"
  port: "8080"
  shutdownTimeout: 5
